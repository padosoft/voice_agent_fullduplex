import type { CanonicalProviderEvent, ConnectionDescriptor, InteractionMode, JsonObject, ProviderUsageInput, RealtimeProviderDriver, ToolResult } from "../types.js";
import { type RealtimeAudioBridge } from "./audio.js";
type WebSocketFactory = (url: string) => WebSocket;
export declare function normalizeGeminiUsage(metadata: JsonObject, providerEventId: string, model: string): ProviderUsageInput;
export declare class GeminiLiveDriver implements RealtimeProviderDriver {
    private readonly request;
    private readonly socketFactory;
    private readonly audio;
    private readonly events;
    private socket;
    private descriptor;
    private connected;
    private readonly seenUsage;
    private readonly callNames;
    private revision;
    private mode;
    private toolNameMap;
    private model;
    private resumeHandle;
    private manuallyClosed;
    private reconnecting;
    private messageSequence;
    private resolveSetup;
    constructor(request?: typeof fetch, socketFactory?: WebSocketFactory, audio?: RealtimeAudioBridge);
    connect(descriptor: ConnectionDescriptor): Promise<void>;
    disconnect(): Promise<void>;
    setMode(mode: InteractionMode): Promise<void>;
    sendText(text: string): Promise<void>;
    sendContext(update: JsonObject): Promise<void>;
    submitToolResult(result: ToolResult): Promise<void>;
    on(listener: (event: CanonicalProviderEvent) => void): () => void;
    private bootstrapAndOpen;
    private handleMessage;
    private handleServerContent;
    private emitTranscript;
    private handleToolCall;
    private handleUsage;
    private seedHistory;
    private reconnect;
    private handleClose;
    private providerNameForCanonical;
    private send;
    private csrfToken;
}
export {};
