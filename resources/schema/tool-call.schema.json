{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agents-full-duplex.dev/schemas/realtime-agent-tool-call@1",
  "title": "Realtime Agent Tool Call v1",
  "type": "object",
  "required": ["id", "name", "arguments", "base_revision", "idempotency_key"],
  "properties": {
    "id": { "type": "string" },
    "name": { "type": "string" },
    "arguments": { "type": "object" },
    "base_revision": { "type": "integer", "minimum": 1 },
    "idempotency_key": { "type": "string" },
    "provider_call_id": { "type": ["string", "null"] }
  },
  "additionalProperties": false
}
